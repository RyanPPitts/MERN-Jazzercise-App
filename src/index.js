import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

// reactdom renders the app information
// loading app into the index.html root
// jsx information is loaded into the html using the render information below
ReactDOM.render(<App />, document.getElementById("root"));
